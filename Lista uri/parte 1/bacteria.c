#include <stdlib.h>
#include <stdio.h>
#include <string.h>


int main(){
    //váriaveis
    char d[101],s[101];

    fgets(d,100,stdin);
    d[strcspn(d,"\n")]='\0';
    setbuf(stdin,NULL);
    fgets(s,100,stdin);
    s[strcspn(s,"\n")]='\0';
    setbuf(stdin,NULL);
    if()

  return 0;
}
